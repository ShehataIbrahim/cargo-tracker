plugins {
    id 'org.springframework.boot' version '2.6.2'
}
apply from: '../build.gradle'
apply from: '../common-lib.gradle'


bootJar {
    manifest {
        attributes 'Start-Class': 'com.streams.tracker.handling.HandlingApplication'
    }
}