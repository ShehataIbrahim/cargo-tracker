spring.config.import=classpath:common.properties
server.servlet.context-path=/handling
spring.application.name=handling

spring.datasource.url=${database-routing-url}/handlingdb
spring.datasource.username=${database-handling-username}
spring.datasource.password=${database-handling-password}